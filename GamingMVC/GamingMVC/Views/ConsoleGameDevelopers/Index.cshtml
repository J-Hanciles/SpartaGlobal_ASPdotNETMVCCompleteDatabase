@model IEnumerable<GamingMVC.ConsoleGameDeveloper>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.stationaryname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.supplier)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Game.gameType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.stationaryname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.supplier)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Game.gameType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.consoleGameDeveloperID }) |
            @Html.ActionLink("Details", "Details", new { id=item.consoleGameDeveloperID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.consoleGameDeveloperID })
        </td>
    </tr>
}

</table>
